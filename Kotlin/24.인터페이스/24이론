 객체지향프로그램
 - 클래스,상속,인터페이스

 인터페이스(Interface) : 정의만하고 구현부가 없다
 Interface Tiger{
     var 줄무늬
     fun bite()
     fun goToBox()
 }

 규약(프로토콜)
 - 인터페이스로 봐주겠다 ==> Tiger타입이다.
 - 인터페이스를 보고 인터페이스를 구현한 클래스의 객체와 의사소통할수 있다.

특징
- 개발자간 협업이 가능(ㄱ은 인터페이스 함수를 구현하고, 다른사람은 그 함수로 로직을 만드는 등등..  ==> 거의 안한다.
- 다중상속은 안되지만 다중 인터페이스 구현은 가능하다.
 . class BakDuSanTiger:Tiger,Bird{}  ==> 비추천